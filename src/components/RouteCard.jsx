import DriverCard from "./DriverCard";
export default function RouteCard({ route, active, onSelect, onHover, onDriverClick }) { return <div className={`route-option ${active ? "selected" : ""}`} style={{ "--route-color": route.color }}><button className="route-card" onClick={() => onSelect(route)} onMouseEnter={() => onHover(route.id)}><span className="route-color" /><span className="route-rank">#{route.rank}</span><span className="route-card-main"><strong>{route.name}</strong><span>{route.rankLabel} <i /> {route.time} · {route.distance}</span>{route.safetyProfile && <small className={`safety-chip safety-${route.safetyProfile.risk.toLowerCase().replace(" ", "-")}`}>Safety {route.safetyProfile.safety}/100 · {route.safetyProfile.badge}</small>}</span><span className="route-card-fare">{route.fare}<small>{route.badge}</small></span></button><DriverCard driver={route.driver} onClick={() => onDriverClick(route.driver)} /></div>; }
