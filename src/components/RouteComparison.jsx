export default function RouteComparison({ routes, activeRoute, onSelect }) { return <section className="comparison-panel"><div className="comparison-heading"><div><span className="eyebrow">Side by side</span><h2>Route comparison</h2></div><span className="muted">Select a route to highlight it</span></div><div className="comparison-scroll"><table><thead><tr><th>Route</th><th>ETA</th><th>Fare</th><th>Safety</th><th>Driver Trust</th><th>Overall Safety</th><th>Badge</th><th>Risk</th></tr></thead><tbody>{routes.map((route) => <tr className={activeRoute === route.id ? "active-row" : ""} key={route.id} onClick={() => onSelect(route)}><td><i style={{ background: route.color }} />{route.name}</td><td>{route.time}</td><td>{route.fare}</td><td>{route.safetyProfile ? `${route.safetyProfile.safety}/100` : "--"}</td><td>{route.driver ? `${route.driver.trust}/100` : "--"}</td><td>{route.overallSafety || "--"}/100</td><td>{route.driver?.badge || "--"}</td><td><span className="risk-indicator">{route.safetyProfile?.risk || "--"}</span></td></tr>)}</tbody></table></div></section>; }
