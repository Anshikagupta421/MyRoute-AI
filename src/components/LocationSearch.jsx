import { FiMapPin, FiSearch } from "react-icons/fi";
export default function LocationSearch({ value, onChange, onChoose, suggestions, activeIndex, onKeyDown, onFocus, onBlur, label, placeholder }) { return <label className="location-field"><span>{label}</span><div className="input-with-icon"><FiMapPin /><input value={value} onChange={onChange} onFocus={onFocus} onKeyDown={onKeyDown} onBlur={onBlur} placeholder={placeholder} aria-autocomplete="list" aria-expanded={suggestions.length > 0} /></div>{suggestions.length > 0 && <div className="suggestion-menu" role="listbox">{suggestions.map((suggestion, index) => <button type="button" role="option" aria-selected={activeIndex === index} key={suggestion} onMouseDown={() => onChoose(suggestion)}><FiSearch /><span>{suggestion}</span></button>)}</div>}</label>; }
