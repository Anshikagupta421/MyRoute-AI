import { FiCheck, FiClock, FiMapPin, FiShield } from "react-icons/fi";
export default function SafetyTimeline({ deviation = false }) { const items = [{ label: "Trip Started", icon: FiClock, state: "complete" }, { label: "Route Monitoring Active", icon: FiMapPin, state: "complete" }, { label: "Driver Verified", icon: FiShield, state: "complete" }, { label: "Destination Reached", icon: FiCheck, state: "pending" }]; return <section className="safety-timeline"><div className="safety-section-heading"><div><span className="eyebrow">Journey protection</span><h2>Trip Safety Timeline</h2></div><span className={`status-label ${deviation ? "status-warning" : ""}`}>{deviation ? "Monitoring route" : "Safe Journey"}</span></div><div className="timeline-list">{items.map(({ label, icon: Icon, state }) => <div className={`timeline-item ${state}`} key={label}><span className="timeline-icon"><Icon /></span><span><strong>{label}</strong><small>{state === "pending" ? "Waiting for trip completion" : "Confirmed in demo mode"}</small></span></div>)}</div></section>; }
