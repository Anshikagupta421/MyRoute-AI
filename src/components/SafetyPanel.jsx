import { FiCheck, FiMoon, FiShield, FiUsers } from "react-icons/fi";
import { getSafetyInsights } from "../services/safetyService";

export function SafetyModeToggle({ enabled, onChange }) { return <section className={`safety-mode-card ${enabled ? "safety-mode-on" : ""}`}><div className="safety-mode-icon"><FiShield /></div><div><span className="eyebrow">Passenger control</span><h2>Women's Safety Mode</h2><p>Prioritize lighting, monitoring, public activity and emergency access.</p></div><button className={`safety-toggle ${enabled ? "on" : ""}`} onClick={() => onChange(!enabled)} aria-pressed={enabled}><span>{enabled ? "ON" : "OFF"}</span><i /></button></section>; }
export function SafetyBanner({ enabled }) { return enabled ? <div className="safety-priority-banner"><FiShield /><strong>Safety Prioritized Recommendation Active</strong><span>Your route choices remain yours.</span></div> : null; }
export function SafetyInsights({ safetyMode, nightTravel }) { return <section className="safety-insights-panel"><div className="insights-title"><FiShield /><div><span className="eyebrow">Decision support</span><h2>AI Safety Insights</h2></div></div><div className="insight-list">{getSafetyInsights(safetyMode, nightTravel).map((insight) => <p key={insight}>{insight}</p>)}</div></section>; }
export function SafetyBreakdown({ profile, nightTravel }) { return <div className="safety-breakdown"><div className="safety-breakdown-heading"><div><span className="eyebrow">Safety Breakdown</span><h3>{profile.badge}</h3></div><strong>{profile.safety}/100</strong></div><div className="safety-factors"><span><FiCheck /> Lighting Quality <b>{profile.lighting}</b></span><span><FiShield /> Police Presence <b>{profile.police}</b></span><span><FiUsers /> Crowd Activity <b>{profile.crowd}</b></span><span><FiShield /> Emergency Access <b>{profile.emergency}</b></span><span><FiCheck /> Monitoring Availability <b>{profile.monitoring}</b></span>{nightTravel && <span><FiMoon /> Night Safety <b>{profile.night}/100</b></span>}</div></div>; }
