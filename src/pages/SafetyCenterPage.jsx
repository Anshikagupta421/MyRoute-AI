import { FiAlertTriangle, FiMapPin, FiMoon, FiShield } from "react-icons/fi";
import { useState } from "react";
import TrustedContacts from "../components/TrustedContacts";
import SafetyTimeline from "../components/SafetyTimeline";
import EmergencyPanel from "../components/EmergencyPanel";
export default function SafetyCenterPage() { const [safetyMode, setSafetyMode] = useState(false); const [emergencyOpen, setEmergencyOpen] = useState(false); return <div className="page safety-center-page"><div className="page-heading"><div><span className="eyebrow">Passenger protection</span><h1>Safety Center</h1><p>Keep your journey support tools close and your choices in your hands.</p></div><div className="safety-center-status"><span /> Safe Journey</div></div><section className="safety-center-grid"><div className={`protection-card ${safetyMode ? "active" : ""}`}><FiShield /><div><span className="eyebrow">Protection preference</span><h2>Women's Safety Mode</h2><p>Prioritize lighting, monitoring and emergency access.</p></div><button className={`safety-toggle ${safetyMode ? "on" : ""}`} onClick={() => setSafetyMode(!safetyMode)}><span>{safetyMode ? "ON" : "OFF"}</span><i /></button></div><div className="protection-card"><FiMoon /><div><span className="eyebrow">Automatic protection</span><h2>Night Travel Protection</h2><p>Activates automatically between 8 PM and 6 AM.</p></div><strong className="protection-ready">Ready</strong></div></section><section className="safety-status-card"><div className="safety-status-icon"><FiShield /></div><div><span className="eyebrow">Current status</span><h2>Safe Journey</h2><p>Route monitoring is ready for your next active trip.</p></div><span className="status-dot-large" /></section><SafetyTimeline /><TrustedContacts /><section className="deviation-card"><div><FiMapPin /><div><span className="eyebrow">Route monitoring</span><h2>Deviation detection</h2><p>Demo alerts will appear here if a route moves significantly away from the expected path.</p></div></div><button className="secondary-action" onClick={() => window.alert("Demo: Route Deviation Detected\nExpected Route: Planned corridor\nCurrent Route: Alternate road\nDeviation Distance: 1.4 km")}><FiAlertTriangle /> Simulate alert</button></section>{emergencyOpen && <EmergencyPanel onClose={() => setEmergencyOpen(false)} />}<button className="safety-center-sos" onClick={() => setEmergencyOpen(true)}><FiAlertTriangle /> SOS</button></div>; }
