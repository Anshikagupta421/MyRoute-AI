import { FiArrowRight, FiImage, FiShield, FiUsers } from "react-icons/fi";
import { useState } from "react";
import { useAuth } from "../context/AuthContext";
import Avatar from "../components/Avatar";
export default function ProfilePage() { const { currentUser, updateUser } = useAuth(); const [form, setForm] = useState({ name: currentUser.name, email: currentUser.email, avatar: currentUser.avatar }); const [message, setMessage] = useState(""); const change = (key) => (e) => setForm({ ...form, [key]: e.target.value }); const upload = (e) => { const file = e.target.files[0]; if (!file) return; const reader = new FileReader(); reader.onload = () => setForm({ ...form, avatar: reader.result }); reader.readAsDataURL(file); }; const submit = (e) => { e.preventDefault(); const issue = updateUser(form); setMessage(issue || "Your profile has been updated."); }; return <div className="page profile-page"><div className="page-heading"><div><span className="eyebrow">Account</span><h1>Your profile</h1><p>Manage the details that make MyRoute yours.</p></div></div><div className="profile-layout"><section className="profile-card card"><div className="profile-card-top"><Avatar user={{ ...currentUser, ...form }} /><div><h2>{currentUser.name}</h2><p>{currentUser.email}</p></div></div><div className="profile-divider" /><div className="profile-meta"><span><FiShield /> Private account</span><span><FiUsers /> Personal workspace</span></div></section><section className="card settings-card"><div className="card-heading"><div><span className="eyebrow">Personal details</span><h2>Profile information</h2></div><span className="edit-label">Editable</span></div><form onSubmit={submit}><label>Full name<input value={form.name} onChange={change("name")} required /></label><label>Email address<input type="email" value={form.email} onChange={change("email")} required /></label><label className="upload-field">Profile picture <span>Optional</span><input type="file" accept="image/*" onChange={upload} /><small><FiImage /> {form.avatar ? "New image selected" : "Upload a new profile picture"}</small></label>{message && <div className={message.includes("updated") ? "form-success" : "form-error"}>{message}</div>}<button className="primary-button" type="submit">Save changes <FiArrowRight /></button></form></section></div></div>; }
